import { DM_Sans, Inter, Montserrat_Alternates } from "next/font/google";

export const inter = Inter({subsets: ['latin']});

export const titleFont = DM_Sans({subsets: ['latin'], weight:['500','700']});
export const secondaryFont = DM_Sans({ subsets: ['latin'], weight: ['400', '700'] });
